;(defn fibonacci [max]
;  (let [sequence (vector 1 1)]
;    (loop [cur 0]
;      (when (< cur max)
;        ()
;        (recur (vector 2 2))
;        )
;      (reduce + sequence)
;      )
;    )
;  )
;
;(prn (fibonacci 1))
(defn fibonacci [max]
  )

(#(

    ) 100)